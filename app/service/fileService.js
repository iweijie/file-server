const config = require("../../config/index")

const file = {
    /*
    * 获取存储的 session
    * @param {String} token      
    * @return {Promise[session]} 承载 session 的 Promise 对象
    */
}

module.exports = file